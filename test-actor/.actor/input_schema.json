{
    "title": "Test Apify Orchestrator Input Schema",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "role": {
            "title": "Role",
            "description": "The role of the Run.",
            "type": "string",
            "enum": ["root", "child"],
            "editor": "select",
            "prefill": "root"
        },
        "numberOfChildren": {
            "title": "Number of Children",
            "description": "The number of child actors to spawn (only applicable if role is 'root').",
            "type": "integer",
            "default": 3
        },
        "childMemoryMbytes": {
            "title": "Child Memory (MB)",
            "description": "The memory limit for each child actor in megabytes (only applicable if role is 'root').",
            "type": "integer"
        },
        "childTaskId": {
            "title": "Child Task ID",
            "description": "The ID of the task to run for each child actor (only applicable if role is 'root'). If not specified, no task will be used.",
            "type": "string",
            "editor": "textfield"
        },
        "orchestratorOptions": {
            "title": "Orchestrator Options",
            "description": "Options for the Orchestrator instance.",
            "type": "object",
            "editor": "json"
        },
        "childWaitSeconds": {
            "title": "Child Wait Seconds",
            "description": "Number of seconds each child actor waits before finishing (only applicable if role is 'child').",
            "type": "integer"
        }
    },
    "required": ["role"]
}
